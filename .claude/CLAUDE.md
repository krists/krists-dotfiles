# Global Preferences

## Ruby / Rails

Always use binstubs instead of `bundle exec`. For example:
- `bin/rubocop` not `bundle exec rubocop`
- `bin/rails` not `bundle exec rails`
- `bin/rspec` not `bundle exec rspec`
- `bin/rake` not `bundle exec rake`
